services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: shopsystem
    networks:
      - shopsystem

networks:
  shopsystem:
    attachable: true
